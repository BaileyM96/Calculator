function add(a, b) {
return a + b;
}

function subtract(a, b) {
return a - b;
}

function multiply(a, b) {
return a * b;
}

function divide(a, b) {
return a / b;
}

//CREATE 3 Variables (3 + 5)
    //SET Number
    //SET Operator number
    //SET Another number

//CREATE fucntion operate();
    //TAKES an operator and 2 numbers
        //THEN Call the above fucntions

function operate(num1, num2, operator) {
    
}